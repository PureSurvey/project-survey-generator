package macros

const (
	ProceederUrl          = "${PROCEEDER_URL}"
	ViewPixelUrl          = "${VIEW_PIXEL_URL}"
	QuestionAnswerTracker = "${QUESTION_ANSWER_TRACKER}"
	QuestionViewTracker   = "${QUESTION_VIEW_TRACKER}"
	SurveyStartTracker    = "${SURVEY_START_TRACKER}"
	SurveyEndTracker      = "${SURVEY_END_TRACKER}"
	UnitEndTracker        = "${UNIT_END_TRACKER}"

	SurveyHeaderText   = "${HEADER_TEXT}"
	CompletedBlockText = "${COMPLETED_TEXT}"
	QuestionBlockText  = "${QUESTION_TEXT}"

	QuestionOptions = "${QUESTION_OPTIONS}"
	UnitItems       = "${UNIT_ITEMS}"
	Styles          = "${STYLES}"

	OptionsType  = "${OPTION_TYPE}"
	OptionsValue = "${OPTION_VALUE}"
	OptionsName  = "${OPTION_NAME}"
	OptionsText  = "${OPTION_TEXT}"

	PrevButton        = "${PREV_BUTTON}"
	PrevButtonText    = "${PREV_BUTTON_TEXT}"
	NextButton        = "${NEXT_BUTTON}"
	NextButtonClasses = "${NEXT_BUTTON_CLASSES}"
	NextButtonText    = "${NEXT_BUTTON_TEXT}"

	UnitId         = "${UNIT_ID}"
	SurveyId       = "${SURVEY_ID}"
	QuestionId     = "${QUESTION_ID}"
	NextQuestionId = "${NEXT_QUESTION_ID}"

	HideUnitAfterEnd = "${HIDE_UNIT_AFTER_END}"

	SurveyPlacement = "${SURVEY_PLACEMENT}"
	Html            = "${HTML}"
	Script          = "${SCRIPT}"
)
